# GEMINI_API_KEY: Optional — powers AI features (optimize, fix bugs, comments, format, auto-naming).
# If not set, users can enter their own key via the Settings tab in the app.
# Get a free key at https://aistudio.google.com/apikey
GEMINI_API_KEY=""
